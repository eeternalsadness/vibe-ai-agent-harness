{
  "name": "vibe-ai-agent-harness",
  "private": true,
  "scripts": {
    "test": "bun test && python3 test/integration/test_pipeline_runner.py -v",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "bun run lint:biome && bun run lint:ruff && bun run lint:shell",
    "lint:biome": "biome lint .",
    "lint:ruff": "ruff check .",
    "lint:shell": "find . -name '*.sh' -not -path './dist/*' -not -path './node_modules/*' -not -path './test/fixtures/*' -print0 | xargs -0 shellcheck"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "1.3.3",
    "@types/bun": "^1.4.2",
    "typescript": "^7.0.2"
  }
}

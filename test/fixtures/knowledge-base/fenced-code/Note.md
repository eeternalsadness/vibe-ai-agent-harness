# Note

Real content, reachable from Index.

Example of a wiki-link inside a code block, which must be ignored by the
dangling-link scan since it's just illustrative, not a real link:

```
[[NotARealLink]]
```
